<turbo-stream action="update" target="createKittieModalBody">
    <template>
        <div class="text-center p-5">
            <h5><i class="fas fa-check-circle" style="color: green;"></i> Say <strong>HEY</strong> to your new friend: <strong>{{ cat.name }}</strong></h5>
        </div>
        <div class="">
            <a class="btn btn-outline-warning" href="{{ path('app_kittie_new', {asStream: true}) }}"><i class="fas fa-plus"></i> Add another</a>
        </div>
    </template>
</turbo-stream>